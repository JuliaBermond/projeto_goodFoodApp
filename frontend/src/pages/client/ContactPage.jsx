import { contactUsContent } from "@/data/siteData";
import TextArea from "@/components/client/TextArea";

const AboutPage = () => {
  return <TextArea content={contactUsContent} />;
};

export default AboutPage;
