import TextArea from "@/components/client/TextArea";
import { aboutUsContent } from "@/data/siteData";

const AboutPage = () => {
  return <TextArea content={aboutUsContent} />;
};

export default AboutPage;
